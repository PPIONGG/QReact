meta {
  name: Get Vendors
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/Master/GetVendors
  body: none
  auth: bearer
}

headers {
  X-PACKAGE: {{package}}
}

auth:bearer {
  token: {{token}}
}

params:query {
  ~search:
  ~pageNumber: 1
  ~pageSize: 50
}

settings {
  encodeUrl: true
  timeout: 0
}
