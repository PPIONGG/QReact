# Developer Glossary (คำศัพท์สำหรับ Developer)

> รวมคำศัพท์ที่ developer ควรรู้
> อัพเดทเรื่อยๆ เมื่อเจอคำใหม่

---

## A

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **Alpine** | Linux distribution ขนาดเล็ก (ใช้ใน Docker) | `postgres:16-alpine` เล็กกว่า `postgres:16` |
| **API** | Application Programming Interface - ช่องทางให้โปรแกรมคุยกัน | Frontend เรียก API เพื่อดึงข้อมูล user |
| **Architecture** | โครงสร้างภาพรวมของระบบ (ใหญ่กว่า Pattern) | Clean Architecture, Microservices |
| **Authentication** | การยืนยันตัวตน (คุณเป็นใคร?) | Login ด้วย email/password |
| **Authorization** | การตรวจสอบสิทธิ์ (คุณทำอะไรได้บ้าง?) | Admin เข้าหน้า /admin ได้ |

---

## B

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **Backend** | ส่วนที่ทำงานบน server (ผู้ใช้มองไม่เห็น) | Go API, Database |
| **Behavior** | การทำงาน/การตอบสนองของโปรแกรมเมื่อมี event | กดปุ่ม → count เพิ่ม |
| **Bcrypt** | Algorithm สำหรับ hash password (ปลอดภัย) | Hash password ก่อนเก็บใน database |
| **Blacklist** | รายการที่ถูกบล็อก | Token ที่ logout แล้ว |
| **Bridge** | Docker network แบบพื้นฐาน (containers คุยกันได้) | `driver: bridge` |
| **Build** | การ compile โค้ดให้พร้อมใช้งาน | `docker build`, `npm run build` |

---

## C

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **Cache** | ที่เก็บข้อมูลชั่วคราว (เร็วกว่า database) | Redis เก็บ session |
| **Clean Architecture** | Architecture ที่แยก layers ชัดเจน (domain, service, handler) | Go API ใช้ Clean Architecture |
| **Container** | กล่องที่บรรจุโปรแกรมพร้อมใช้งาน | Docker container |
| **Context** | ตัวส่งข้อมูล/ควบคุม lifecycle ข้าม function | Go context, React Context |
| **Cookie** | ข้อมูลเล็กๆ ที่เก็บใน browser | refresh_token cookie |
| **CORS** | Cross-Origin Resource Sharing - อนุญาตให้เว็บอื่นเรียก API | Frontend (3000) เรียก API (8080) |
| **CRUD** | Create, Read, Update, Delete - 4 การทำงานพื้นฐาน | สร้าง/อ่าน/แก้ไข/ลบ user |

---

## D

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **Database** | ฐานข้อมูล | PostgreSQL, MySQL, MongoDB |
| **Dependency Injection** | Pattern ส่ง dependency จากข้างนอกเข้ามา (ไม่สร้างเอง) | `NewService(repo)` แทน `service.repo = NewRepo()` |
| **Deploy** | การนำโค้ดไปรันบน server จริง | Deploy ขึ้น production |
| **Docker** | เครื่องมือสร้าง container | `docker-compose up` |
| **Domain** | ชื่อเว็บไซต์ | google.com, localhost |

---

## E

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **Endpoint** | URL ที่ API รับ request | `/api/auth/login` |
| **Environment** | สภาพแวดล้อมการทำงาน | development, production |
| **ENV / Environment Variable** | ตัวแปรสำหรับตั้งค่า | `DATABASE_URL`, `PORT` |

---

## F

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **Frontend** | ส่วนที่ผู้ใช้เห็นและโต้ตอบ | Next.js, React |
| **Framework** | โครงสร้างพื้นฐานที่ช่วยเขียนโค้ด | Next.js, Gin (Go) |

---

## G

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **Gin** | Web framework ยอดนิยมสำหรับ Go (เร็วมาก) | `gin.Default()` |

---

## H

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **Handler** | Function ที่รับ HTTP request และตอบ response | `func LoginHandler(c *gin.Context)` |
| **Hash** | การเข้ารหัสแบบทางเดียว (ถอดรหัสไม่ได้) | Hash password ด้วย bcrypt |
| **Healthcheck** | การเช็คว่า service ทำงานปกติ | Docker healthcheck |
| **Hot Reload** | อัพเดทโค้ดโดยไม่ต้อง restart | แก้โค้ด → เห็นผลทันที |
| **HTTP** | โปรโตคอลสื่อสารบนเว็บ | GET, POST, PUT, DELETE |
| **HttpOnly** | Cookie ที่ JavaScript อ่านไม่ได้ (ปลอดภัยกว่า) | refresh_token cookie |

---

## I

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **Image** | แม่แบบสำหรับสร้าง container | `postgres:16-alpine` |
| **Implement** | ลงมือเขียนโค้ดจริงตามที่ออกแบบ | implement feature ตาม design |
| **In-memory** | เก็บข้อมูลใน RAM (เร็วมาก) | Redis เก็บใน RAM |
| **Industry Standard** | มาตรฐานที่ทุกคนในวงการใช้กัน | Redis เป็น industry standard สำหรับ cache |
| **Interface** | กำหนด "สัญญา" ว่า type ต้องมี method อะไรบ้าง | `type UserRepository interface { ... }` |

---

## J

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **JSON** | รูปแบบข้อมูลที่นิยมใช้ใน API | `{"name": "pingpong"}` |
| **JWT** | JSON Web Token - token สำหรับ authentication | access_token, refresh_token |

---

## L

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **Latency** | ความหน่วง (เวลาที่ใช้ในการตอบกลับ) | Redis มี latency ต่ำ |
| **Library** | โค้ดสำเร็จรูปที่เอามาใช้ได้เลย | bcrypt, jwt-go |
| **Localhost** | เครื่องคอมพิวเตอร์ของตัวเอง | http://localhost:3000 |

---

## M

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **Middleware** | โค้ดที่ทำงานก่อน/หลัง request หลัก | Auth middleware เช็ค token |
| **Migration** | การเปลี่ยนแปลงโครงสร้าง database | สร้าง table users |
| **Monorepo** | เก็บหลาย project ใน repo เดียว | apps/web + apps/api ใน repo เดียว |
| **Mount** | การเชื่อมต่อ folder/file เข้ากับ container | `./apps/api:/app` |

---

## N

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **Network** | เครือข่ายที่ให้ containers คุยกัน | `pingpong-network` |

---

## O

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **ORM** | Object-Relational Mapping - เขียน database ด้วยโค้ด | Prisma, GORM |

---

## P

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **Pattern** | วิธีการแก้ปัญหาที่ใช้กันทั่วไป | Repository Pattern |
| **preventDefault()** | หยุด/ป้องกันพฤติกรรมเริ่มต้นของ element | `e.preventDefault()` หยุด form refresh |
| **Payload** | ข้อมูลที่ส่งไปกับ request | `{"email": "...", "password": "..."}` |
| **Port** | ช่องทางเข้าถึง service | 8080 (API), 5432 (PostgreSQL) |
| **Production** | สภาพแวดล้อมจริงที่ผู้ใช้เข้าถึง | เว็บไซต์ที่ deploy แล้ว |

---

## R

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **RBAC** | Role-Based Access Control - จัดการสิทธิ์ตาม role | admin, user |
| **Redis** | In-memory database สำหรับ cache | เก็บ token, session |
| **Repository** | ส่วนที่ติดต่อกับ database | UserRepository |
| **Request** | การร้องขอจาก client | GET /api/users |
| **Response** | การตอบกลับจาก server | `{"users": [...]}` |
| **REST API** | รูปแบบการออกแบบ API ที่นิยม | GET, POST, PUT, DELETE |

---

## S

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **Schema** | โครงสร้างของข้อมูล | Prisma schema, database schema |
| **Seed** | การใส่ข้อมูลเริ่มต้นใน database | สร้าง default roles |
| **Service** | ส่วนที่จัดการ business logic | AuthService |
| **Session** | ช่วงเวลาที่ user login อยู่ | เก็บใน Redis |
| **SQL** | ภาษาสำหรับจัดการ database | SELECT * FROM users |
| **Struct** | โครงสร้างข้อมูลใน Go (คล้าย class) | `type User struct { ID string }` |

---

## T

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **Token** | รหัสสำหรับยืนยันตัวตน | JWT access_token |
| **TTL** | Time To Live - เวลาหมดอายุ | Token หมดอายุใน 15 นาที |
| **Turborepo** | เครื่องมือจัดการ monorepo (เร็ว, cache ได้) | `turbo run build` |

---

## V

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **Validation** | การตรวจสอบความถูกต้องของข้อมูล | Email ต้องมี @, Password 8 ตัวขึ้นไป |
| **Volume** | ที่เก็บข้อมูลถาวรของ Docker | `./data/postgres` |

---

## W

| คำศัพท์ | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| **Webhook** | URL ที่รับแจ้งเตือนอัตโนมัติ | แจ้งเมื่อมี payment |

---

## สัญลักษณ์ที่พบบ่อย

| สัญลักษณ์ | ความหมาย |
|-----------|----------|
| `:` | แยก port (เครื่อง:container) เช่น `5432:5432` |
| `/` | แยก path เช่น `/api/auth/login` |
| `@` | at - ใช้ใน email, database URL |
| `?` | query parameter เช่น `?token=xxx` |
| `&` | เชื่อม query parameters เช่น `?a=1&b=2` |
| `#` | fragment/anchor หรือ comment |
| `$` | ตัวแปร environment เช่น `$PORT` |
| `*` | wildcard (ทุกอย่าง) |

---

## คำย่อที่พบบ่อย

| คำย่อ | เต็ม | ความหมาย |
|-------|------|----------|
| **API** | Application Programming Interface | ช่องทางให้โปรแกรมคุยกัน |
| **CLI** | Command Line Interface | โปรแกรมที่ใช้ผ่าน terminal |
| **CRUD** | Create, Read, Update, Delete | 4 การทำงานพื้นฐาน |
| **DB** | Database | ฐานข้อมูล |
| **ENV** | Environment | สภาพแวดล้อม |
| **GUI** | Graphical User Interface | หน้าจอที่มี UI |
| **HTTP** | HyperText Transfer Protocol | โปรโตคอลสื่อสารบนเว็บ |
| **JSON** | JavaScript Object Notation | รูปแบบข้อมูล |
| **JWT** | JSON Web Token | token สำหรับ auth |
| **ORM** | Object-Relational Mapping | เขียน DB ด้วยโค้ด |
| **RBAC** | Role-Based Access Control | จัดการสิทธิ์ตาม role |
| **REST** | Representational State Transfer | รูปแบบ API |
| **SQL** | Structured Query Language | ภาษาจัดการ database |
| **TTL** | Time To Live | เวลาหมดอายุ |
| **UI** | User Interface | หน้าจอผู้ใช้ |
| **URL** | Uniform Resource Locator | ที่อยู่เว็บ |

---

*เอกสารนี้จะอัพเดทเรื่อยๆ เมื่อเจอคำศัพท์ใหม่* 📚